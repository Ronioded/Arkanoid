/* Roni Oded. ID-318798782. */
package tasks;

/**
 * interface Task - represent a task that can run after a selection in the menu.
 * @author Roni Oded.
 */
public interface Task {

    /**
     * the method run the task after a selection in the menu.
     */
    void run();
}